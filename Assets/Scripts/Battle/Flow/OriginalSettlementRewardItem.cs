using System;
using UnityEngine;

namespace BackpackHero.Battle
{
    /// <summary>
    /// Empty migration marker kept at the legacy source path until Unity regenerates
    /// its generated compilation input. Reward rendering now uses ItemReward directly.
    /// </summary>
    [Obsolete("Reward rendering uses ItemReward directly.", false)]
    public sealed class OriginalSettlementRewardItem : MonoBehaviour
    {
    }
}
